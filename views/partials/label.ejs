<div class="grid h-full bg-white pt-6 pb-6 px-2 border-4 border-black">
  <div>
    <p class="text-xs font-semibold text-slate-400">
      <%= label.quantity %> tasks
    </p>
    <p><%= label.name %></p>
  </div>
  <div class="w-full h-1 mt-auto" style="background-color: <%= label.color %>" ></div>
</div>
